"use server";
import { Appointment } from "@/model/appointment.model";

const add_appointment = async (body: Appointment) => {
  try {
  } catch (err) {
    console.error(err);
    return null;
  }
};
